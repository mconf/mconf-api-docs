get:
  summary: API Version
  operationId: api-get-version
  description: Returns the API version
  tags:
    - extra
  responses:
    "200":
      description: Successful operation
      content:
        application/xml:
          examples:
            $ref: "../components/examples/version-success.yaml"
          schema:
            $ref: "../components/schemas/responses/version.yaml"
post:
  summary: API Version
  operationId: api-post-version
  description: Returns the API version
  tags:
    - extra
  responses:
    "200":
      description: Successful operation
      content:
        application/xml:
          examples:
            $ref: "../components/examples/version-success.yaml"
          schema:
            $ref: "../components/schemas/responses/version.yaml"
head:
  summary: API Version
  operationId: api-head-version
  description: Returns the API version
  tags:
    - extra
  responses:
    "200":
      description: Successful operation
