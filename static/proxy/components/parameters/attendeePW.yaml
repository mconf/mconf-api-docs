name: attendeePW
in: query
description: The password that will [join](#operation/join) URL can later provide as its password parameter to indicate the user will as a attendee. If no `attendeePW` is provided, create will return a randomly generated `attendeePW` password for the meeting. 
schema:
  type: string