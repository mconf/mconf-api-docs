type: object
required:
  - response
properties:
  response:
    type: object
    required:
      - returncode
      - nextPage
      - meetings
    properties:
      returncode:
        type: string
      nextPage:
        type: boolean
      meetings:
        type: array
        items:
          type: object
          $ref: '../meetingData.yaml'
